#include "main.h"

/**
 * _abs - returns the absolute value of
 * a given number
 * @c: the number to be evaluated
 * Return: the absolute value of the number
 */




int _abs(int c)
{
	if (c >= 0)
	{
		return (c);
	}
	return (-1 * c);
}

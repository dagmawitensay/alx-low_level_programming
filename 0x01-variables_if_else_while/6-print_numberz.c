#include <stdio.h>
#include <stdlib.h>

/**
 * main - a function that prints digits
 * from zero to nine
 * Return: 0
 */

int main(void)
{
	unsigned int num = 0;

	while (num <= 9)
	{
		putchar(i);
		num++;
	}
	putchar('\n');
	return (0);
}

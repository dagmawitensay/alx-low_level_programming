#include "main.h"
#include <stdio.h>

/**
 * main - funct that prints name of the program
 * @argc: number of arguments
 * @argv: pointer to an array of pointers
 * Return: 0 sucess
 */

int main(int __attribute__ ((unused)) argc, char **argv)
{
	printf("%s\n", argv[0]);

	return (0);
}
